#include <stdio.h>
/*
Process: it is a running program
Often we deployed hundreds of process at once. The OS creates the illusion by
virtualizing the CPU (running  one process, then stopping it and running another)
We achive that using a basic technique known as time sharing. To implement 
virtualization well we need low-level machinery (mechanisims) and high level
intelligence (policies).

*/

int main(int argc, char *argv[]){



    return 0;
}